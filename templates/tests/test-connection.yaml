apiVersion: v1
kind: Pod
metadata:
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  name: '{{ include "project3.fullname" . }}-test-connection'
  labels:
    {{ include "project3.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "project3.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
