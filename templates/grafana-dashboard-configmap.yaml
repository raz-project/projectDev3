apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard
  namespace: {{ .Values.namespace }}
  labels:
    grafana_dashboard: "1"
data:
  complex-app-dashboard.json: |
    {{ .Files.Get "dashboards/grafana-dashboard.json" | indent 4 }}
